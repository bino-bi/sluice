# Default-deny means bare pass-through needs an explicit allow;
# the assertion here is that nothing rewrites the statement.
apiVersion: sluice.bino.bi/v1alpha1
kind: SqlAccessPolicy
metadata:
  name: allow-all
  priority: 0
spec:
  effect: allow
  match:
    any:
      - resources:
          tables: ["*"]
